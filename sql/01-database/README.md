# 01 Database

Implemented file: `01-create-clientiq-database.sql`.

Creates the ClientIQ SQL Server database, applies the Simple recovery model, and creates the `clientiq` and `reporting` schemas. The script safely skips objects that already exist.
