# 02 Tables

Planned file: `01-create-core-tables.sql`.

Creates the six approved MVP tables, technical keys, compatible constraints, and indexes. Source identifiers involved in intentional duplicate, missing-reference, and orphan tests must remain loadable so the data-quality procedure can detect them.
