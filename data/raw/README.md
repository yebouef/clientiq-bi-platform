# Raw Synthetic Data

This folder will contain the complete generated CSV source files:

- `clients.csv`
- `attorneys.csv`
- `matters.csv`
- `time_entries.csv`
- `invoices.csv`

The files must contain synthetic information only. Controlled defects will remain in these files so the SQL data-quality engine can detect them.

